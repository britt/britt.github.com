Home-page inventory of things built. Descriptions are sentences, not marketing.

```jsx
<ProjectList items={[{ name: "lotsofcsvs.com", href: "…", description: "a small data warehouse built with CSV and HTTP as its API." }]} />
```
