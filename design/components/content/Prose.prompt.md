Wrap any run of authored content. Headings, lists, quotes and code inside it inherit the global element styles.

```jsx
<Prose><p>These are things I built because I wanted them to exist.</p></Prose>
```

Pass `measure={false}` when the child is a grid or list that should fill the page.
