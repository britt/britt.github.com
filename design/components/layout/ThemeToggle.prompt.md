Lowercase word button ("dark" / "light") that flips the page color scheme.

```jsx
<ThemeToggle scheme={scheme} onChange={setScheme} />
```

Never use a sun/moon icon — this system labels the destination in words.
