How every page fills the viewport without stretching body copy into unreadable lines.

```jsx
<PageShell aside={<ProfileHeader align="start" … />}>
  <Prose>…</Prose>
</PageShell>
```

The rail carries identity or metadata only — never primary reading content.
