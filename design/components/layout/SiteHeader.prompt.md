The only chrome at the top of every page — a lowercase monospace wordmark that links home.

```jsx
<SiteHeader scheme={scheme} onSchemeChange={setScheme} onNavigate={go} />
```

There is no logo mark; the wordmark is plain type. Pass `showToggle={false}` for print or embed contexts.
