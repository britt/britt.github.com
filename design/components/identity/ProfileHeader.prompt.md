The home page masthead. Left-align it (`align="start"`) when it lives in a `PageShell` rail.

```jsx
<ProfileHeader align="start" name="Britt Crawford"
  tagline={<>Working to build <a href="https://www.sandgarden.com">Sandgarden</a>.</>}
  links={[{ label: "GitHub", href: "…" }]} />
```
