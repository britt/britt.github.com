The one photographic element in the system — the profile portrait at the top of the home page.

```jsx
<Avatar src="/assets/portrait.jpg" alt="Britt Crawford" />
```

No border, no shadow, no ring. Use `initials` when no image is available.
